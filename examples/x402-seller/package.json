{
  "name": "@mainspring/example-x402-seller",
  "version": "0.1.0",
  "private": true,
  "description": "Runnable proof of a governed x402 SELLER: an agent operates paid HTTP endpoints, settles simulated USDC payments offline, ledgers every sale, and lets @mainspring/governance BLOCK an over-cap price change and a refund that no policy rule authorizes — offline, zero credentials.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "tsc -p tsconfig.json && node dist/main.js",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "dependencies": {
    "@mainspring/governance": "workspace:*",
    "@mainspring/ledger": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  }
}
