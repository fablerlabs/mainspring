{
  "name": "@mainspring/example-x402-buyer",
  "version": "0.1.0",
  "private": true,
  "description": "Governed x402 buyer: a spendGate (per-action + daily caps, built as @mainspring/governance Rules) approves or blocks micro-purchases before payment, ledger records every settled spend.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "tsc -p tsconfig.json && node dist/main.js",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js"
  },
  "dependencies": {
    "@mainspring/governance": "workspace:*",
    "@mainspring/ledger": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.2"
  }
}
